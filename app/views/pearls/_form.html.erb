<%= form_with(model: pearl, local: true) do |form| %>
  <% if pearl.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pearl.errors.count, "error") %> prohibited this pearl from being saved:</h2>

      <ul>
      <% pearl.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :pearl_name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :pearl_description %>
  </div>

  <div class="field">
    <%= form.label :picture %>
    <%= form.text_field :picture, id: :pearl_picture %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
